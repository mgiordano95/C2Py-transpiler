#include <stdio.h>

int main() {
    if () {
        printf("We are inside the if statement\n"); //ifStatement_Error (Error: missing condition in 'if statement')
    } else if () {
        printf("We are inside the else if statement\n"); //elseifStatement_Error (Error: missing condition in 'else if statement')
    } else {
        printf("We are inside the else statement\n");
    }
    
    while () {
        printf("We are inside the while statement\n"); //whileLoop_Error (Error: missing condition in 'while statement')
    }
    
    return 0;
}