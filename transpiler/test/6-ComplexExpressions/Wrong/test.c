#include <stdio.h>

int main() {
    int x = 3;
    int y = 7;
    
    if ( y > x + ) { //ComplexExpressions_Error (syntax error)
        printf("y is greater than x plus 3");
    }
    
    return 0;
}